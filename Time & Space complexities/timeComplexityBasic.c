#include <stdio.h>

int main()
{
    int n1, n2, sum;

    n1 = 10; // assigned 10 into n1 variable
    n2 = 20; // same for n2 variable

    sum = n1 + n2; //applying addition of two variables & assign it in sum

    return 0;
}
